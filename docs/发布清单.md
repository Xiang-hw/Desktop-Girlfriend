# 私有分发清单

本清单用于当前私有专用版本的复制、打包和交付前核对。自动测试不能替代素材授权、隐私检查与人工视觉验收。

## 源码与测试

- [x] Python 3.12 环境可以安装运行依赖；
- [x] 完整自动测试通过；
- [x] Windows 本地构建脚本会检查私有素材门禁；
- [x] Python 模块具有中文模块文档字符串。

## 文件与隐私

- [x] `user_assets/` 除说明文件外全部被 Git 忽略；
- [x] `.venv/`、`build/`、`dist/` 和 Python 缓存均被忽略；
- [x] 当前构建包含 `user_assets/pet/` 中已验收的私有角色帧；
- [x] 待提交文本中不存在真实照片路径、账号、密钥和临时文件路径；
- [x] 自拍功能只读取用户最初登记的原图；
- [x] 首次提交前再次执行 `git add --dry-run .` 并人工查看文件清单。

## 角色与素材

- [x] 标准角色必须通过图形确认窗口由用户确认；
- [x] 候选文件哈希变化后旧确认自动失效；
- [x] 走路 GIF 需要第二次用户确认；
- [x] 表情符号由程序图层绘制，不依赖某一套角色图片；
- [x] `ASSETS_LICENSE.md` 声明当前角色、自拍、动作帧和图标为私有素材；
- [x] 已删除项目内公开演示素材和演示素材处理工具。

## 私有包验证

- [ ] 运行 `scripts/build.ps1` 生成私有包；
- [ ] 在干净目录解压并启动验证；
- [ ] 确认包内不含原始照片、工作草稿、缓存和本机临时路径；
- [ ] 只交付给已获授权的使用者。
