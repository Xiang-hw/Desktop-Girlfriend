# 文件夹说明

## 1. 文件夹用途

本文件夹是“一图桌宠（OnePic Desktop Pet）”的私有专用版本，用于运行已经通过人工验收的专属女孩桌宠、维护私有素材处理流程、测试和打包脚本。

## 2. 背景与目标

项目已从公开演示版切换为私有素材版。运行时只加载 `user_assets/pet/manifest.json`，图标也来自 `user_assets/pet/icon.png`。真实照片、自拍图、动作图集和运行帧都保存在被 Git 忽略的 `user_assets/` 内。

## 3. 文件结构

| 路径 | 类型 | 作用 |
|---|---|---|
| `main.py` | Python 入口 | 解析参数并启动桌宠 |
| `src/onepic_desktop_pet/` | Python 包 | 应用生命周期、窗口、行为、配置、资源定位和确认门禁 |
| `user_assets/pet/` | 私有运行素材 | 应用直接加载的透明动作帧、manifest 和图标 |
| `user_assets/generated/` | 私有派生输入 | 重新拆分动作帧所需的最终透明图集 |
| `user_assets/` | 本地素材 | 原图、自拍、标准角色候选、动作预览和流程状态，默认不提交 Git |
| `tools/onepic_workflow.py` | Python 工具 | 登记原图并管理标准角色和走路两个人工确认门禁 |
| `src/onepic_desktop_pet/emotion_effects.py` | Python 模块 | 为任意角色绘制可缩放的表情符号增强层 |
| `scripts/` | PowerShell | 环境检查、安装、运行、测试和打包 |
| `agent-guide/` | 文档 | Agent 从一张图执行项目的固定流程 |
| `tests/` | Python 测试 | 验证配置、行为、素材和窗口逻辑 |
| `OnePicDesktopPet.spec` | 构建配置 | 生成 Windows 目录模式可执行程序 |
| `技术更新.md` | 维护记录 | 记录结构、技术和发布边界的变化 |

## 4. 核心流程

环境检查 → 登记原图和自拍 → 选择画风并分析身份特征 → 生成首张标准角色候选 → 用户在确认窗口确认 → 生成动作 → 走路 GIF 确认 → 自动测试 → 最终验收 → 私有打包 → 隐私与授权检查。

## 5. 重要约束

- 所有角色、自拍和运行素材都放在 `user_assets/`；
- 不在代码中硬编码本机绝对路径；
- 不自动安装系统软件或修改环境变量；
- 标准角色未确认时不生成动作，走路未确认时不加载或打包本地宠物；
- 自拍必须使用最初登记的原图；
- 私有包不得包含原始照片、临时路径或未授权素材；
- 不从私人项目复制 `build/`、`dist/`、缓存、归档和错误草稿。

## 6. Agent 接手建议

先阅读本文件、`技术更新.md`、`AGENTS.md` 和 `agent-guide/AGENT_GUIDE.md`。修改 Python 文件前先读模块文档字符串；运行环境操作优先调用 `scripts/`；分发前重点检查 `user_assets/`、`ASSETS_LICENSE.md` 和构建目录。

## 7. 当前状态

当前专属女孩动作和自拍素材已经通过最终人工验收。项目内公开演示素材、演示图标、演示处理工具已经删除；应用、测试和打包脚本只使用 `user_assets/` 下的私有素材。
